import Foundation

enum WatchStrings {
    static var cancel: String {
        NSLocalizedString("common.cancel", comment: "Cancel")
    }

    static var syncFailed: String {
        NSLocalizedString("error.sync_failed", comment: "Sync failed")
    }
}
